# Install basic tools

pkg install python
pkg install vim # or nano for simpler editing

# For a more IDE-like experience:

pkg install neovim python-pip
pip install pynvimpkg install python python-pip
pip install prompt_toolkit pygments
